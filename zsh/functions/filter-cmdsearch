local selected
selected=$({ _cmdsearch_snippet_records; _cmdsearch_history_records; } \
        | $FZF $FZF_OPTS --read0 --query "$LBUFFER")
if [ -n "$selected" ]; then
    BUFFER=$selected
    CURSOR=$#BUFFER
fi
zle clear-screen
